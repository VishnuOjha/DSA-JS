Revision DSA: 

1-> a function which search for element in the array and return the index of it.
	
	for so we have an array = [1,6,9,8,7,,3]
	element = 7

	function findNumberIndex(arr, element){

		let index = 0;
		for(let i = 0; i< arr.length; i++){
				if(arr[i] == element)
				retrun i
		}
		return -1
	}


	2-> write a function which return number of negative number in the array

	for so we have an array = [1,-6,9,-8,7,-3]


	function findNegativeNumber(arr){

		let count = 0;
		for(let i = 0; i< arr.length; i++){
				if(num > 0)
				retrun 0
				else
				count = count + 1
		}
		return count
	}


3-> find largest and smallest number in the array

array = [1,6,9,8,7,8,3]

let largest = -Infinity
let smallest= Infinity

for(let i= 0; i< arr.length; i++){
	
	if(arr[i] > largest){
		largest = arr[i]
	}
	
	if(arr[i] < smallest){
	smallest = arr[i]
	}

	retrun {largest, smallest}
}


4-> find the second largest value in the array
array = [1,6,9,8,7,8,3]

corner case :
-> array is empty
-> array has duplicates
-> array has negative numbers

function findSecondLargest(arr){
	
	if(arr.length < 2) return null

	let firstLargest = -Infinity
	let secondLargest = -Infinity


	for(i = 0; i< arr.length; i++){
		if(arr[i] > firstLargest){

			secondLargest = firstLargest
			firstLargest = arr[i]
		}
		else if(arr[i] > secondLargest && arr[i] != firstLargest){
		secondLargest = arr[i]
		}

	}

	return secondLargest
}



5-> count digits in number

function countDigit(num){
	
	// if num has 0 value
	if(n ==0) return 1

// if num has negative value
	num = Math.abs(num)

	let count = 0

	while(n > 0){

		n = math.floor(n/10)
		count++
	}

	return count
}



6-> palindrome number

// when any integer can be read same from forward and backward is pelindrom number
// if reverse of any number == number
// pelindrom is any number or string

// in any number if you want to find the lst digit use : n % 10
// remove last digit from number : n / 10


function isPalindrome(n){
	
// if num has 0 value
if(n < 0) retrun false

let rev = 0;
let copy = n

while(n>0){
	let rem = rem % 10;
	rev = (rev * 10) + rem
	n = math.floor(n/10)
}


retrun rev == copy
}


7-> reverse integer

function reverseNumber(n){
	
	let copy = n
	// if n is negative number
	n = Math.abs(n)

	let rev = 0

	while(n>0){
	let rem = rem % 10
	rev = (rev * 10) + rem
	n = math.floor(n/10)
	}

	f(rev < - (2**32) || rev > (2**31)) return 0
   return nCopy < 0 ? -rev : rev
}

8-> remove duplicates from the array

function removeDuplicates(){
	
let x= 0
for(i=0; i< n.length; i++){
	
	if(a[i] > a[x]){
	x= x+1;
	a[x] = a[i]
	}
}
return X+ 1
}